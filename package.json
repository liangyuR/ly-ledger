{
  "name": "ly-ledger",
  "version": "0.1.0",
  "private": true,
  "description": "烟酒店进销存台账 —— NocoBase 后端 + 自研极简前端，本地单机运行",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "migrate": "npm run migrate -w @ly-ledger/server",
    "start": "npm run start -w @ly-ledger/server",
    "dev": "npm run dev -w @ly-ledger/server",
    "typecheck": "npm run typecheck -w @ly-ledger/server",
    "verify:model": "npm run verify:model -w @ly-ledger/server"
  },
  "license": "UNLICENSED"
}
