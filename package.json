{
  "name": "ly-ledger",
  "version": "0.1.0",
  "private": true,
  "description": "烟酒店进销存台账 —— Tauri + Rust + SQLite + React，本地单机桌面应用",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "web:dev": "npm run dev -w @ly-ledger/web",
    "web:build": "npm run build -w @ly-ledger/web",
    "test": "cargo test --manifest-path packages/desktop/Cargo.toml",
    "typecheck": "npm run typecheck -w @ly-ledger/web && cargo check --manifest-path packages/desktop/Cargo.toml --all-targets"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  }
}
