# 当初用 npm 不是偏好，是被 NocoBase 逼的：它发布的 registry 元数据里
# `deprecated` 字段是数组（规范要求字符串），pnpm 的严格解析器会因此丢弃
# 整个版本条目，831/1132 个版本全都装不上（详见 docs/03 的 M0 踩坑存档）。
#
# NocoBase 在 M1 就被推翻了，这条理由跟着一起没了，所以换回 pnpm。

audit=false
fund=false
