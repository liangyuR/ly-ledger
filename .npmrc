# 用 npm 而不是 pnpm —— 不是偏好，是被逼的：
# NocoBase 发布的 registry 元数据里 `deprecated` 字段是数组（规范要求字符串），
# pnpm v12 的严格解析器会因此丢弃整个版本条目，831/1132 个版本全都装不上。
# npm 的解析器宽容，能正常装。详见 docs/03。
#
# 附带好处：npm workspaces 天然是扁平 node_modules，
# 正合 NocoBase 按包名动态 require 插件的需要。

audit=false
fund=false
